services:
  # Backend Web Service
  - type: web
    name: chat-backend
    runtime: python
    region: oregon
    plan: free
    rootDir: backend
    buildCommand: |
      python -m pip install -U pip
      pip install -r requirements.txt
    startCommand: python init_db.py && gunicorn --worker-class eventlet -w 1 run:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.10
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: chat-db
          property: connectionString
      # Placeholder for frontend URL (update after first frontend deploy)
      - key: CORS_ORIGINS
        value: https://your-frontend-url.onrender.com

  # Frontend Static Site
  - type: web
    name: chat-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && CI=false npm run build
    staticPublishPath: build
    # Handle React Router client-side routing
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      # Placeholder for backend URL (update after first backend deploy)
      - key: REACT_APP_API_URL
        value: https://your-backend-url.onrender.com
      - key: REACT_APP_SOCKET_URL
        value: https://your-backend-url.onrender.com

# PostgreSQL Database
databases:
  - name: chat-db
    region: oregon
    plan: free
    databaseName: chatapp
    user: chatuser
